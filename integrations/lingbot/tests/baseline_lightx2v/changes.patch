diff --git a/lightx2v/models/runners/wan/wan_lingbot_fast_runner.py b/lightx2v/models/runners/wan/wan_lingbot_fast_runner.py
index 60d5bde5..b410040b 100755
--- a/lightx2v/models/runners/wan/wan_lingbot_fast_runner.py
+++ b/lightx2v/models/runners/wan/wan_lingbot_fast_runner.py
@@ -1,6 +1,7 @@
 import torch
 from loguru import logger
 
+from flashdreams.infra.profiler import EventProfiler
 from lightx2v.common.kvcache import KVCacheManager
 from lightx2v.models.networks.wan.lingbot_fast_model import WanLingbotFastModel
 from lightx2v.models.runners.wan.wan_runner import LingbotRunner, WanRunner, build_wan_model_with_lora
@@ -63,7 +64,7 @@ class LingbotFastRunner(LingbotRunner):
     def get_video_segment_num(self):
         self.video_segment_num = self.scheduler.num_chunks
 
-    def run_segment(self, segment_idx=0):
+    def run_segment(self, segment_idx=0, segment_profiler=None):
         infer_steps = self.model.scheduler.infer_steps
         for step_index in range(infer_steps):
             if self.video_segment_num == 1:
@@ -85,6 +86,8 @@ class LingbotFastRunner(LingbotRunner):
                 total_all_steps = self.video_segment_num * infer_steps
                 self.progress_callback((current_step / total_all_steps) * 100, 100)
 
+        if segment_profiler is not None:
+            segment_profiler.record("denoise")
         return self.model.scheduler.stream_output
 
     def init_run(self):
@@ -117,8 +120,9 @@ class LingbotFastRunner(LingbotRunner):
                 metrics_labels=["DefaultRunner"],
             ):
                 self.check_stop()
+                segment_profiler = EventProfiler()
                 self.init_run_segment(segment_idx)
-                latents = self.run_segment(segment_idx)
+                latents = self.run_segment(segment_idx, segment_profiler=segment_profiler)
                 all_latents.append(latents)
 
                 with ProfilingContext4DebugL1("step_pre_in_rerun"):
@@ -130,6 +134,26 @@ class LingbotFastRunner(LingbotRunner):
                 with ProfilingContext4DebugL1("infer_main_in_rerun"):
                     self.model.infer(self.inputs)
 
+                segment_profiler.record("kv_update")
+                stages_ms = segment_profiler.sync_and_summarize()
+                total_ms = sum(stages_ms.values())
+                total_ms_wo_finalize = total_ms - stages_ms.get("kv_update", 0.0)
+                stages_str = " ".join(
+                    f"{stage} {ms:.2f}ms" for stage, ms in stages_ms.items()
+                )
+                print(
+                    f"Chunk {segment_idx} completed in {total_ms:.2f}ms "
+                    f"(wo_finalize {total_ms_wo_finalize:.2f}ms, "
+                    f"x0 shape {tuple(latents.shape)}) | {stages_str}"
+                )
+                logger.info(
+                    "Chunk {} completed in {:.2f}ms (wo_finalize {:.2f}ms, x0 shape {}) | {}",
+                    segment_idx,
+                    total_ms,
+                    total_ms_wo_finalize,
+                    tuple(latents.shape),
+                    stages_str,
+                )
                 torch.cuda.empty_cache()
 
         all_latents = torch.cat(all_latents, dim=1)
diff --git a/lightx2v/pipeline.py b/lightx2v/pipeline.py
index fbba300b..9dcd37c8 100755
--- a/lightx2v/pipeline.py
+++ b/lightx2v/pipeline.py
@@ -16,12 +16,18 @@ except (ImportError, ModuleNotFoundError) as e:
     logger.warning(f"Flux2 runners not available: {e}")
 from lightx2v.models.runners.hunyuan_video.hunyuan_video_15_runner import HunyuanVideo15Runner  # noqa: F401
 from lightx2v.models.runners.longcat_image.longcat_image_runner import LongCatImageRunner  # noqa: F401
-from lightx2v.models.runners.ltx2.ltx2_runner import LTX2Runner  # noqa: F401
+try:
+    from lightx2v.models.runners.ltx2.ltx2_runner import LTX2Runner  # noqa: F401
+except Exception as e:  # noqa: BLE001
+    logger.warning(f"LTX2 runner not available: {e}")
 from lightx2v.models.runners.neopp.neopp_runner import NeoppRunner  # noqa: F401
 from lightx2v.models.runners.qwen_image.qwen_image_runner import QwenImageRunner  # noqa: F401
 from lightx2v.models.runners.seedvr.seedvr_runner import SeedVRRunner  # noqa: F401
 from lightx2v.models.runners.wan.wan_animate_runner import WanAnimateRunner  # noqa: F401
-from lightx2v.models.runners.wan.wan_audio_runner import Wan22AudioRunner, WanAudioRunner  # noqa: F401
+try:
+    from lightx2v.models.runners.wan.wan_audio_runner import Wan22AudioRunner, WanAudioRunner  # noqa: F401
+except Exception as e:  # noqa: BLE001
+    logger.warning(f"Wan audio runners not available: {e}")
 from lightx2v.models.runners.wan.wan_distill_runner import WanDistillRunner  # noqa: F401
 from lightx2v.models.runners.wan.wan_lingbot_fast_runner import LingbotFastRunner  # noqa: F401
 from lightx2v.models.runners.wan.wan_matrix_game2_runner import WanSFMtxg2Runner  # noqa: F401
@@ -29,10 +35,16 @@ from lightx2v.models.runners.wan.wan_matrix_game3_runner import WanMatrixGame3Ru
 from lightx2v.models.runners.wan.wan_runner import Wan22MoeRunner, WanRunner  # noqa: F401
 from lightx2v.models.runners.wan.wan_sf_runner import WanSFRunner  # noqa: F401
 from lightx2v.models.runners.wan.wan_vace_runner import WanVaceRunner  # noqa: F401
-from lightx2v.models.runners.worldmirror.worldmirror_runner import WorldMirrorRunner  # noqa: F401
-from lightx2v.models.runners.worldplay.worldplay_ar_runner import WorldPlayARRunner  # noqa: F401
-from lightx2v.models.runners.worldplay.worldplay_bi_runner import WorldPlayBIRunner  # noqa: F401
-from lightx2v.models.runners.worldplay.worldplay_distill_runner import WorldPlayDistillRunner  # noqa: F401
+try:
+    from lightx2v.models.runners.worldmirror.worldmirror_runner import WorldMirrorRunner  # noqa: F401
+except Exception as e:  # noqa: BLE001
+    logger.warning(f"WorldMirror runner not available: {e}")
+try:
+    from lightx2v.models.runners.worldplay.worldplay_ar_runner import WorldPlayARRunner  # noqa: F401
+    from lightx2v.models.runners.worldplay.worldplay_bi_runner import WorldPlayBIRunner  # noqa: F401
+    from lightx2v.models.runners.worldplay.worldplay_distill_runner import WorldPlayDistillRunner  # noqa: F401
+except Exception as e:  # noqa: BLE001
+    logger.warning(f"WorldPlay runners not available: {e}")
 from lightx2v.models.runners.z_image.z_image_runner import ZImageRunner  # noqa: F401
 from lightx2v.utils.input_info import init_empty_input_info, update_input_info_from_dict
 from lightx2v.utils.registry_factory import RUNNER_REGISTER
